# Production environment configuration for Cloud Run deployment
# When deployed, the React app will be served by the same server as the API
# So we use relative URLs to point to the same origin

# API Base URL - use relative path since served from same origin
REACT_APP_API_URL=

# WebSocket URL - use relative path, will be converted to wss:// in production
REACT_APP_WS_URL=

# Build configuration
GENERATE_SOURCEMAP=false
