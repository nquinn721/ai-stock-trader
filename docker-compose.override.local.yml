version: "3.8"

# =============================================================================
# DOCKER COMPOSE OVERRIDE - LOCAL PRODUCTION TESTING
# =============================================================================
#
# This file provides overrides for testing the production Docker setup locally
# with different ports to avoid conflicts with development servers.
#
# Usage:
#   docker-compose -f docker-compose.prod.yml -f docker-compose.override.local.yml up
#
# Port Mappings (Local Production):
#   - Backend:  localhost:8100 (instead of 8000)
#   - Frontend: localhost:3100 (instead of 3000)
#   - Database: localhost:3307 (instead of 3306)
#
# Environment: trading_app_local_prod database
# =============================================================================

# COMMENTED OUT - Uncomment when needed for local production testing
services:
  # backend-prod:
  #   ports:
  #     - "8100:8000" # Different port for local testing
  #   environment:
  #     - DATABASE_URL=mysql://trading_user:trading_pass@database-prod:3306/trading_app_local_prod

  # frontend-prod:
  #   ports:
  #     - "3100:80" # Different port for local testing
  #   build:
  #     args:
  #       - REACT_APP_API_URL=http://localhost:8100

  # database-prod:
  #   ports:
  #     - "3307:3306" # Different port for local testing
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=root_password
  #     - MYSQL_DATABASE=trading_app_local_prod
  #     - MYSQL_USER=trading_user
  #     - MYSQL_PASSWORD=trading_pass
